version: '3'

services:
  libra_db:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: example
  libra_web:
    build: .
    entrypoint: /code/entrypoint.sh
    volumes:
      - .:/code
    ports:
      - "8003:8003"
    depends_on:
      - libra_db
